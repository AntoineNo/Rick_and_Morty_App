<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rick_morty">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.rick_morty.presentation.RickAndMortyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RickMorty">
        <activity
            android:name="com.example.rick_morty.presentation.characterDisplay.CharacterActivity"
            android:label="@string/character_detail_activity"
            android:theme="@style/Theme.RickMorty.NoActionBar"></activity>
        <activity
            android:name="com.example.rick_morty.presentation.characterDisplay.MainActivity"
            android:configChanges="orientation"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.RickMorty.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>